<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kacper_kontny_wt_15_30">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kacper_Kontny_wt_15_30">
        <activity
            android:name=".CalcActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".GoldActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".HistoricRatesActivity"
            android:parentActivityName=".CurrencyViewsActivity" />
        <activity
            android:name=".CurrencyViewsActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>